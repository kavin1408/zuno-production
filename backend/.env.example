# Database Configuration
# For local development: defaults to SQLite (sqlite:///./zuno_v2.db)
# For production (Railway): use Supabase PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@host:port/dbname

# Authentication
# Get this from your Supabase project settings > API > JWT Secret
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# AI Service
# Get API key from OpenRouter: https://openrouter.ai/
OPENROUTER_API_KEY=your-openrouter-key

# Server Configuration
# Port for the backend server (Railway will set this automatically)
PORT=8000

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# For production, add your Vercel frontend URL
# Example: https://your-app.vercel.app,http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
